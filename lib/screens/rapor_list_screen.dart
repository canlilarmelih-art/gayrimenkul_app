import 'package:flutter/material.dart';

class RaporListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Rapor Listesi")),
      body: Center(child: Text("Raporlar burada listelenecek")),
    );
  }
}
